.row
  = form_tag "/auth/twitter", :method => "get", :html => {:class => "form-horizontal"} do
    %fieldset
      %legend
        = t("app.description")
      .form-actions
        %button.btn.btn-large.btn-primary
          = t("button.sign_in")
